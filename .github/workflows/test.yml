name: test

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GHCR_TOKEN }}
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build, tag, and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: _2_services/healt_center_web_page/docker
        push: true
        tags: |
          ghcr.io/${{ github.repository }}/healt_center_web_page:latest
        platforms: linux/amd64,linux/arm64
        labels: |
          maintainer=fberton98
          version=1.0.0

  build1:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GHCR_TOKEN }}
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build, tag, and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: _2_services/new_heartbeata_endpoint/docker
        push: true
        tags: |
          ghcr.io/${{ github.repository }}/new_heartbeata_endpoint:latest
        platforms: linux/amd64,linux/arm64
        labels: |
          maintainer=fberton98
          version=1.0.0

  build3:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GHCR_TOKEN }}
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build, tag, and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: _2_services/epilepsy_prediction/docker
        push: true
        tags: |
          ghcr.io/${{ github.repository }}/epilepsy_prediction:latest
        platforms: linux/amd64,linux/arm64
        labels: |
          maintainer=fberton98
          version=1.0.0
  
  build4:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GHCR_TOKEN }}
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build, tag, and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: _2_services/new_eeg_data_endpoint/docker
        push: true
        tags: |
          ghcr.io/${{ github.repository }}/new_eeg_data_endpoint:latest
        platforms: linux/amd64,linux/arm64
        labels: |
          maintainer=fberton98
          version=1.0.0